# Инструкция по отправке на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку "New repository" или "+" → "New repository"
3. Заполните данные репозитория:
   - **Repository name**: `ds-betta-2`
   - **Description**: `Discord Clone - Modern alternative to Discord with real-time messaging, voice channels, and modern UI`
   - **Visibility**: Public (или Private)
   - **Initialize**: Не добавляйте README, .gitignore или license (они уже созданы)

4. Нажмите "Create repository"

## Шаг 2: Настройка Git репозитория

После создания репозитория на GitHub, выполните следующие команды в терминале:

```bash
# Перейдите в папку проекта (если еще не там)
cd discord-clone

# Удалите старый remote (если есть)
git remote remove origin

# Добавьте новый remote с вашим GitHub URL
git remote add origin https://github.com/YOUR_USERNAME/ds-betta-2.git

# Замените YOUR_USERNAME на ваше имя пользователя GitHub

# Отправьте код на GitHub
git push -u origin master
```

## Шаг 3: Проверка

После выполнения команд:
1. Перейдите по ссылке `https://github.com/YOUR_USERNAME/ds-betta-2`
2. Убедитесь, что все файлы загружены корректно
3. Проверьте, что README.md отображается на главной странице

## Альтернативный способ (GitHub CLI)

Если у вас установлен GitHub CLI:

```bash
# Создание репозитория через CLI
gh repo create ds-betta-2 --public --description "Discord Clone - Modern alternative to Discord"

# Отправка кода
git push -u origin master
```

## Структура файлов в репозитории

После успешной загрузки в репозитории должны быть:

```
ds-betta-2/
├── README.md                    # Описание проекта
├── .gitignore                   # Git ignore файл
├── .env.example                 # Пример переменных окружения
├── package.json                 # Корневой package.json
├── tsconfig.json                # TypeScript конфигурация
├── docker-compose.yml           # Docker сервисы
├── docs/
│   └── architecture.md          # Архитектурная документация
├── shared/                      # Общие типы и утилиты
│   ├── package.json
│   ├── tsconfig.json
│   └── src/
│       ├── types.ts             # TypeScript типы
│       ├── constants.ts         # Константы
│       ├── utils.ts             # Утилиты
│       └── index.ts             # Главный файл
└── backend/                     # Backend сервер
    ├── package.json
    ├── tsconfig.json
    ├── prisma/
    │   └── schema.prisma        # Схема базы данных
    └── src/
        └── index.ts             # Главный файл сервера
```

## Следующие шаги

После загрузки на GitHub:

1. **Fork репозитория** (опционально) для собственной разработки
2. **Создайте ветки** для новых функций: `git checkout -b feature/new-feature`
3. **Создавайте Pull Request** для предложения изменений
4. **Используйте Issues** для отслеживания задач и багов

## Полезные Git команды

```bash
# Проверка статуса
git status

# Добавление изменений
git add .
git add specific-file.js

# Создание commit
git commit -m "Add new feature"

# Отправка на GitHub
git push origin branch-name

# Переключение на ветку
git checkout branch-name

# Создание новой ветки
git checkout -b feature-name

# Слияние веток
git merge branch-name

# Обновление локального репозитория
git pull origin master
```

## Поддержка

Если у вас возникли проблемы с отправкой на GitHub:
1. Проверьте, что у вас есть права на запись в репозиторий
2. Убедитесь, что URL репозитория корректный
3. Проверьте подключение к интернету
4. При необходимости используйте GitHub Desktop или VSCode интеграцию

---

**Удачной разработки Discord Clone! 🚀**