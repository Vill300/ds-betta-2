# Discord Clone

Современный аналог Discord с реалтайм сообщениями, голосовыми комнатами и современным UI.

## 🚀 Особенности

- **Реалтайм сообщения** - WebSocket соединения для мгновенного обмена сообщениями
- **Голосовые комнаты** - WebRTC для P2P аудио связи
- **Система серверов** - Создание и управление серверами (гильдиями)
- **Текстовые каналы** - Категории, каналы с markdown поддержкой
- **Роли и права** - Гибкая система ролей с granular правами
- **Файлообмен** - Загрузка файлов через S3/MinIO
- **Уведомления** - Web Push и email уведомления
- **Поиск** - Полнотекстовый поиск по сообщениям
- **Модерация** - Бан, мут, логи модерации

## 🏗 Архитектура

### Backend
- **Node.js** + **Express.js** - API сервер
- **PostgreSQL** + **Prisma** - База данных и ORM
- **Redis** - Кеширование и сессии
- **Socket.IO** - WebSocket соединения
- **MinIO** (S3-совместимое) - Файловое хранилище
- **Coturn** - TURN сервер для WebRTC

### Frontend
- **React 18** + **TypeScript** - UI библиотека
- **Vite** - Build инструмент
- **Tailwind CSS** - Стилизация
- **Zustand** - Управление состоянием
- **Socket.IO Client** - WebSocket клиент

### Infrastructure
- **Docker** + **Docker Compose** - Контейнеризация
- **Nginx** - Reverse proxy
- **JWT** - Аутентификация

## 🛠 Быстрый старт

### Предварительные требования
- Node.js 18+
- Docker и Docker Compose
- Git

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/ds-betta-2.git
cd ds-betta-2
```

2. Установите зависимости:
```bash
npm install
```

3. Скопируйте переменные окружения:
```bash
cp .env.example .env
```

4. Запустите Docker сервисы:
```bash
npm run docker:up
```

5. Настройте базу данных:
```bash
npm run db:migrate
npm run db:seed
```

6. Запустите development серверы:
```bash
npm run dev
```

### Структура команд

- `npm run dev` - Запустить все сервисы в development
- `npm run dev:backend` - Запустить только backend
- `npm run dev:frontend` - Запустить только frontend
- `npm run build` - Собрать production версии
- `npm run test` - Запустить тесты
- `npm run lint` - Проверить код линтером

### Docker сервисы

Приложение запускает следующие сервисы:
- **PostgreSQL** (порт 5432) - Основная база данных
- **Redis** (порт 6379) - Кеш и сессии  
- **MinIO** (порты 9000, 9001) - Файловое хранилище
- **Coturn** (порт 3478) - TURN сервер для WebRTC
- **Nginx** (порты 80, 443) - Reverse proxy

## 📁 Структура проекта

```
discord-clone/
├── docs/                    # Документация
│   └── architecture.md     # Архитектурная документация
├── shared/                  # Общие типы и утилиты
│   ├── src/types.ts        # TypeScript типы
│   ├── src/constants.ts    # Константы
│   └── src/utils.ts        # Утилитарные функции
├── backend/                 # Backend сервер
│   ├── src/                # Исходный код
│   ├── prisma/             # Схема базы данных
│   └── package.json        # Backend зависимости
├── frontend/                # Frontend приложение
│   ├── src/                # Исходный код React
│   └── package.json        # Frontend зависимости
├── docker-compose.yml       # Docker сервисы
├── .env.example            # Пример переменных окружения
└── README.md               # Этот файл
```

## 🔧 Конфигурация

Основные переменные окружения (см. `.env.example`):

- `DATABASE_URL` - Строка подключения к PostgreSQL
- `REDIS_URL` - Строка подключения к Redis
- `JWT_SECRET` - Секретный ключ для JWT токенов
- `S3_ENDPOINT` - Endpoint для S3/MinIO
- `CORS_ORIGIN` - Разрешенные origins для CORS

## 🗄 База данных

Проект использует PostgreSQL с Prisma ORM. Основные модели:

- **User** - Пользователи системы
- **Server** - Серверы (гильдии)
- **Channel** - Каналы в серверах
- **Message** - Сообщения в каналах
- **Role** - Роли для серверов
- **Attachment** - Прикрепленные файлы

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена
- `POST /api/auth/logout` - Выход

### Пользователи
- `GET /api/users/me` - Текущий пользователь
- `PUT /api/users/me` - Обновление профиля
- `POST /api/users/me/avatar` - Загрузка аватара

### Серверы
- `GET /api/servers` - Список серверов пользователя
- `POST /api/servers` - Создание сервера
- `GET /api/servers/:id` - Информация о сервере
- `PUT /api/servers/:id` - Обновление сервера

### Каналы
- `GET /api/servers/:serverId/channels` - Каналы сервера
- `POST /api/servers/:serverId/channels` - Создание канала
- `GET /api/channels/:id` - Информация о канале

### Сообщения
- `GET /api/channels/:channelId/messages` - Сообщения канала
- `POST /api/channels/:channelId/messages` - Отправка сообщения
- `PUT /api/messages/:id` - Редактирование сообщения
- `DELETE /api/messages/:id` - Удаление сообщения

## 🔌 WebSocket Events

### Аутентификация
- `authenticate` - Аутентификация через WebSocket

### Сообщения
- `message:send` - Отправка сообщения
- `message:new` - Новое сообщение
- `message:edit` - Редактирование сообщения
- `message:delete` - Удаление сообщения

### Присутствие
- `presence:update` - Обновление статуса
- `typing:start` - Начало печати
- `typing:stop` - Остановка печати

### Голос
- `voice:join` - Присоединение к голосовому каналу
- `voice:leave` - Выход из голосового канала
- `voice:offer` - WebRTC предложение
- `voice:answer` - WebRTC ответ

## 🧪 Тестирование

```bash
# Backend тесты
cd backend && npm run test

# Frontend тесты  
cd frontend && npm run test

# Все тесты
npm run test
```

## 🚀 Деплой

### Docker Production

```bash
# Сборка production образов
docker-compose -f docker-compose.prod.yml build

# Запуск production сервисов
docker-compose -f docker-compose.prod.yml up -d
```

### Manual Deployment

1. Соберите проекты:
```bash
npm run build
```

2. Установите production зависимости:
```bash
cd backend && npm install --production
cd frontend && npm install --production
```

3. Настройте production переменные окружения

4. Запустите сервисы:
```bash
npm run start:backend
npm run start:frontend
```

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

**Discord Clone** - Современная альтернатива Discord с открытым исходным кодом.